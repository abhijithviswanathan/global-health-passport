# Android emulator uses http://10.0.2.2:8080; iOS simulator uses localhost.
# Physical devices need the development computer's LAN IP. Never use HTTP for real records.
EXPO_PUBLIC_API_URL=http://localhost:8080
