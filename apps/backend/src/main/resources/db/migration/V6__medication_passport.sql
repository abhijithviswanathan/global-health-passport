CREATE TABLE medication_passport(internal_pk BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,id VARCHAR(36) UNIQUE NOT NULL,patient_id VARCHAR(36) NOT NULL REFERENCES app_user(id),reference_hash VARCHAR(64) UNIQUE NOT NULL,credential TEXT NOT NULL,expires_at VARCHAR(40) NOT NULL,created_at VARCHAR(40) NOT NULL);
