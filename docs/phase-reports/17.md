# Phase 17: System validation

Status: See source-of-truth and final verification evidence; full phase completion not asserted.

Created/reviewed: Final verification report must distinguish executed passes, failures and NOT VERIFIED checks.

Files: TESTING.md; TRACEABILITY.md

Decisions: retain complete requested scope; use a synthetic adult outpatient engineering boundary. User's latest instruction authorizes continuous execution without repeated phase approval prompts.

Tests executed/results: documentation review only in this report. Application test results must be linked from the final verification report; no pass is inferred here.

Security findings and risks: production identity, jurisdictional policy, clinical validation, operational controls and unimplemented requirements remain explicit gates. Refer to SECURITY, COMPLIANCE and PRD backlog.

Inspection: read the files above and use the root README's current executable commands. Next work is the next numbered phase and outstanding acceptance cases; later activity does not automatically close this phase's gaps.

## Final backend evidence
Maven package succeeded on Java 21/Spring Boot 3.5.16: 29 discovered tests, 23 executed passed, 6 optional PostgreSQL cases skipped, 0 failures/errors. H2 migrations V1–V8, identity, passkeys, consent/workflow, documents, medication credentials, learning, FHIR and organization cases are represented. See evidence/backend-test-summary.json. Project dependency OSV scan covered 148 artifacts with zero matches after remediation. Native devices, real ClamAV, production infrastructure/load/PITR/blob/key restore and independent penetration review remain NOT VERIFIED. Final browser/npm evidence subsequently passed: seven Playwright tests against the static build/live API, plus type/build/lint/format checks and zero npm audit findings. Earlier browser launch blockers were superseded.

Subsequent real-engine evidence: PostgreSQL17.11 applied V1–V8 and passed all six inherited workflow cases with no skips. Logical application-data restoration matched 13 tables, 7 identity sequences and 36 audit-chain events. This supersedes the initial PostgreSQL sandbox blocker; it does not extend PostgreSQL coverage to every feature-specific H2 test. See backend POSTGRES_VERIFICATION.md.
